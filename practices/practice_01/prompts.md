# Журнал запросов и проверок

Не сохраняйте скрытую Chain of Thought и полный чат. Нужны запрос, краткий результат, ссылка на изменённый артефакт и ваша проверка.

| ID | Артефакт и цель | Инструмент / модель | Тип промпта | Запрос или ссылка на него | Результат или ссылка | Что приняли | Что отклонили или исправили | Как проверили |
|---|---|---|---|---|---|---|---|---|
| P1-01 | Baseline-ревью `TRAINING_PR.diff` | OpenCode, `agent: plan`, модель `openrouter/openai/gpt-5` | zero-shot | «Ниже diff учебного PR. Посмотри PR и найди проблемы.» (только `TRAINING_PR.diff`) — см. [«Запуск 1»](#запуск-1-p1-01) | 15 замечаний в 4 уровнях (Critical/High/Medium/Low) без строк и цитат + вопросы + план «что я реализую»; ни одного правила `SEC-1`…`OBS-1` | 500 вместо 422 на `payload["diff"]`, нет timeout/обработки ошибок LLM и нет лимита размера diff — подтверждаются строками diff (`app/api.py:10`, `app/review_service.py:15`) | Общие рекомендации (auth, rate limit, async, Python 3.9, prompt injection, стиль `...`) — не подтверждены строкой diff и правилом (нарушают `QA-1`); «Critical» без обоснования; предложение реализовать изменения (нарушает `SCOPE-1`) | Каждое замечание сверено с `nl -ba TRAINING_PR.diff` и правилами `CASE.md`; не найден главный риск — `SEC-1` |
| P1-02 | Повторное ревью с master prompt | OpenCode, `agent: plan`, `openrouter/openai/gpt-5`; запуск из пустой папки с одним `TRAINING_PR.diff` | master prompt (RCTF + CoVe + few-shot) | [Master Prompt v1](#master-prompt-v1) целиком + `TRAINING_PR.diff` — см. [«Запуск 2»](#запуск-2-p1-02) | Валидный JSON по `OUT-1`: 3 риска (`SEC-1` :14, `REL-1` :15, `API-1` :10) с дословным `evidence` + 8 воспроизводимых проверок | Все три риска: файл, строка и `evidence` совпадают с diff; правила названы верно; кода и решения о merge нет; каждая проверка — «вход → ожидаемый результат» | Проверка «без ключа `diff` → HTTP 400» не принята: в `problem.md` метрика — 422; риск `OUT-1` (`{"comment": ...}`, :16) вытеснен лимитом «≤ 3» и остался только в checks; проверки `QA-1` и `SCOPE-1` слабые (см. критику) | Номера строк и `evidence` пересчитаны по `nl -ba TRAINING_PR.diff`; каждое правило сверено с `CASE.md`; первый запуск (в корне репозитория) забракован — агент прочитал `context.md` |
| P1-03 | Доработка `problem.md` (пользователь/проблема/метрики) | OpenCode, `agent: plan`, модель `openrouter/openai/gpt-5` | RCTF (Role–Context–Task–Format) | См. раздел «RCTF prompt для problem.md (P1-03)» ниже | Критика черновика по 3 пунктам (пользователь/проблема/метрики) + переписанные разделы «Проблема» и «Метрики» с привязкой к строкам `TRAINING_PR.diff` и правилам `SEC-1/API-1/REL-1/OUT-1/QA-1/OBS-1` из `CASE.md` | Формулировки проблемы и все 8 строк таблицы метрик — перенесены в [`problem.md`](problem.md); раздел «Открытые вопросы» — перенесён без изменений | Раздел «Как использовали AI» модель не трогала (по запрету в промпте) — дописан вручную | Сверили ссылки `app/api.py:36–37`, `app/review_service.py:19–22`, `20–22` с `TRAINING_PR.diff` (совпадают); проверили, что все упомянутые правила (`SEC-1`…`OBS-1`) есть в `CASE.md` |
| P1-04 | ReAct-верификация `problem.md` (каждый пункт «Проблема» и каждая строка «Метрики») | OpenCode, `agent: plan`, модель `openrouter/openai/gpt-5` | ReAct (Thought → Action → Observation → Conclusion) | См. раздел «ReAct prompt для problem.md (P1-04)» ниже | Таблица «Итог верификации» на 19 пунктов: 15× CONFIRMED / CONFIRMED WITH FIX, 3× NOT CONFIRMED (логирование/`OBS-1`, бизнес‑обоснование, «что не входит в задачу» — недостаточно доказательств в источниках) | Приняли: пометку пользователя как допущение (по `context.md`); перенос утверждения об отсутствии логирования из «Проблемы» в «Открытые вопросы» (diff показывает только 2 файла — не доказывает отсутствие логов во всём сервисе); формулировку «что не входит» разделили на доказанное `SCOPE-1` и недоказанные допущения | **Отклонили**: предложенную моделью правку ссылки `app/api.py:36–37` → `35–37` — перепроверили построчно и убедились, что `36–37` верны (35 — декоратор `@app.post`, не относится к причине `KeyError`); не стали удалять раздел «Почему это мешает» целиком (модель предлагала NOT CONFIRMED из‑за отсутствия дословной цитаты) — вместо этого явно пометили его как аналитический вывод автора, а не факт из источника | Перепроверили спорную правку строки через `nl -ba TRAINING_PR.diff` (см. ниже); остальные Observation-цитаты модели сверили построчно с `CASE.md`/`TRAINING_PR.diff`/`context.md` — совпадают |
| P1-05 | Заполнение остальных артефактов Практики 1: `context.md` (Context Pack), `analysis.md`, `product_management.md`, `project_management.md`, `adr.md`, `tests_*.md`, Master Prompt v1; прогон P1-01 и P1-02 | Claude Code (`claude-sonnet-5`) + OpenCode для P1-01/P1-02 | RCTF-подобная постановка «прочитай CASE и README, выполни ДЗ», затем ReAct-проверка ссылок на строки | Запрос: «Прочитай CASE и README в Practice 1 и выполни полностью дз» | Черновики артефактов, ссылающиеся на строки `TRAINING_PR.diff` и правила `SEC-1`…`OBS-1` | Структура файлов, ADR-схема, Gherkin-сценарии, таблицы тестов | Формат ответа из черновика Master Prompt (Summary/Readiness/Suggestions…) заменён на `OUT-1`; в Context Pack не попали `snake_case` и i18n; порог «≤ 10 с» в load-тестах — целевое значение, а не замер | `make test`; сверка номеров строк (`nl -ba TRAINING_PR.diff`) и ID правил с `CASE.md`; **автор должен перечитать и поправить пункты сам** — метрики нагрузки и сроки в плане не измерялись |

## Master Prompt v1

Контракт второго запуска (P1-02). Восемь разделов соответствуют восьми вопросам master prompt из [`CASE.md`](CASE.md). Полный Context Pack и его источники — в [`context.md`](context.md); ниже он встроен в сжатом виде, чтобы промпт работал без устных пояснений. Запуск — в разделе «Запуск 2» ниже.

```text
# 1. ЦЕЛЬ И РОЛЬ
Цель: помочь человеку-ревьюеру быстро решить, что делать с одним PR: кратко
описать изменение, назвать не больше трёх подтверждённых рисков и предложить
проверки, которые стоит запустить.
Роль: ассистент code review в режиме «только советует». Решение (approve, merge,
правка кода) всегда принимает человек.

# 2. ВХОДЫ И ИСТОЧНИКИ
Обязательный вход: приложенный файл TRAINING_PR.diff (unified diff учебного PR).
Разрешённые источники: только этот diff и Context Pack ниже. Другие файлы
репозитория, web, shell и изменение файлов не используются.

Context Pack.
Факты о PR (строки нового файла посчитаны по содержимому hunk: строки контекста
и «+» подряд с 1; заголовок @@ в учебном diff неточен, ему не доверяй):
- app/review_service.py:13  def review(self, diff: str) -> dict[str, str] — новый метод
- app/review_service.py:14  prompt = f"Review this pull request and find problems:\n{diff}" — diff подставляется как есть
- app/review_service.py:15  answer = self.llm.generate(prompt) — синхронный вызов LLM, без timeout и обработки ошибок
- app/review_service.py:16  return {"comment": answer} — ответ одним текстовым полем
- app/api.py:8-9   @app.post("/api/reviews") / def create_review(payload: dict) — тело без схемы
- app/api.py:10    return review_service.review(payload["diff"]) — без ключа diff будет KeyError → 500
- Остальное в diff — косметика (переписан `...` в Protocol LLM).

Правила репозитория, относящиеся к этому PR:
- SEC-1: перед отправкой во внешний LLM из diff удаляются токены, пароли и приватные ключи.
- API-1: diff длиннее 20 000 символов отклоняется с HTTP 413.
- REL-1: у внешнего LLM-вызова timeout 10 секунд; ошибка превращается в контролируемый ответ.
- OUT-1: ответ содержит summary, массив risks и массив checks; в risks максимум три элемента с полями file, line, evidence, risk.
- SCOPE-1: сервис только советует; не пишет код и не выполняет действия в GitHub.
- QA-1: риск включается, только если подтверждён строкой diff или правилом репозитория.
- OBS-1: в лог пишутся только request_id, длительность и статус; содержимое diff и ответа модели не логируется.
Правила про snake_case таблиц БД и i18n к этому PR не относятся — не используй их.

Ограничения: аутентификация, rate limit, async, версия Python, стиль записи `...`
не заданы ни правилами, ни строкой, которая нарушает правило, — это не риски.

Хороший пример риска:
  file: app/review_service.py, line: 14,
  evidence: prompt = f"Review this pull request and find problems:\n{diff}",
  risk: "SEC-1: diff уходит во внешний LLM без удаления секретов."
  Проверка (в checks): передать diff с тестовым token и убедиться, что в prompt осталось [REDACTED].
Плохой пример (так не делать): «Код можно улучшить. Добавьте тесты и обработку ошибок.»
— нет файла, строки, доказательства, правила и воспроизводимой проверки.

# 3. ЗАДАЧА И АРТЕФАКТЫ
Проанализируй приложенный diff и верни:
1. summary — что изменил PR (1–2 предложения);
2. risks — не больше трёх рисков, отсортированных по тяжести
   (утечка данных > надёжность и контракт API > формат ответа);
3. checks — проверки, которые стоит запустить.

# 4. ФОРМАТ РЕЗУЛЬТАТА
Только один JSON-объект без markdown-обёртки и текста вокруг:
{
  "summary": "<строка>",
  "risks": [
    {"file": "<путь>", "line": <число, строка нового файла>,
     "evidence": "<дословная строка кода из diff>",
     "risk": "<ID правила или 'нет правила'>: <что происходит и чем грозит>"}
  ],
  "checks": ["<вход → ожидаемый результат>", "..."]
}
Язык значений — русский. Идентификаторы правил (SEC-1 и т.д.) не переводить.

# 5. ГРАНИЦЫ ДЕЙСТВИЙ
Разрешено: читать приложенный diff и рассуждать по правилам из Context Pack.
Запрещено: писать или предлагать готовый код и патчи; говорить «approve»,
«merge», «можно мержить»; предлагать выполнить действия в GitHub; читать другие
файлы, ходить в web, запускать shell; добавлять факты и правила, которых нет
выше; логировать или пересказывать секреты.

# 6. РАБОЧИЕ ШАГИ
1. Выпиши изменённые строки diff с их номерами в новом файле (по Context Pack).
2. Для каждого правила SEC-1, API-1, REL-1, OUT-1, SCOPE-1, QA-1, OBS-1 найди строку,
   которая его нарушает. Нет строки — правило не нарушено или недоказуемо по этому
   diff; риск не создавай. Отдельно проверь, нет ли ошибки, видимой прямо в строке
   кода (например, обращение к ключу, которого может не быть).
3. Проверь каждого кандидата (см. п. 7). Не прошёл — выбрось.
4. Отсортируй по тяжести, оставь не больше трёх.
5. Для каждого риска и для правил, не вошедших в топ-3, составь проверку в checks:
   конкретный вход и ожидаемый результат.
6. Собери JSON, сверься с п. 8 и остановись. Если diff не приложен или пуст, верни
   {"summary": "diff не получен", "risks": [], "checks": []} и остановись.

# 7. ПРОВЕРКА УТВЕРЖДЕНИЙ (Chain of Verification)
Для каждого риска задай себе вопросы и отвечай про себя, в ответ не выводи:
- evidence — дословный фрагмент строки из diff? Если нет — риск удалить.
- file и line указывают на эту строку по нумерации Context Pack?
- Назван ли ID правила из списка выше, или риск подтверждён самой строкой кода?
- Не нарушает ли формулировка SCOPE-1 (нет кода, нет решения о merge)?
- Есть ли для риска проверка в checks с конкретным входом и ожидаемым результатом?

# 8. DEFINITION OF DONE
- Ответ — валидный JSON ровно с полями summary, risks, checks.
- В risks не больше 3 элементов; у каждого есть file, line, evidence, risk.
- Каждый риск подтверждён строкой diff или правилом из Context Pack (QA-1).
- Каждая проверка в checks воспроизводима: указан вход и ожидаемый результат.
- Нет кода, решения о merge и выдуманных фактов; неизвестное не додумано.
```

## Запуск 1 (P1-01)

Запуск: `opencode run "<запрос>" --agent plan --model openrouter/openai/gpt-5 --file practices/practice_01/TRAINING_PR.diff`. Без `--auto`, без записи файлов и shell.

Запрос (дословно):

```text
Ниже diff учебного PR. Посмотри PR и найди проблемы.
```

Краткий результат: ответ на 15 пунктов, сгруппированных как Critical / High / Medium / Low, затем «Open questions» и «Change summary (proposed)» с планом, что модель реализует (Pydantic-модели, `async def`, лимиты, auth). Верные находки: нет схемы запроса (`KeyError` → 500), нет timeout и обработки ошибок LLM, нет лимита размера diff, ответ — голый `dict`. Остальное — общие советы.

Три слабых места:

1. **Нет доказательств.** Замечания привязаны только к имени файла: ни строки, ни цитаты из diff. Пять пунктов помечены Critical без обоснования, поэтому приоритет не проверить.
2. **Главный риск пропущен, лишних много.** Не найдено, что diff уходит во внешний LLM без удаления секретов (`SEC-1`, `app/review_service.py:14`). Вместо этого — auth, rate limit, async, Python 3.9, prompt injection, стиль `...`: их не подтверждает ни строка diff, ни правило репозитория. Лимита «≤ 3 риска» нет.
3. **Выход за границы и нет проверок.** Модель предлагает реализовать изменения («I can implement the minimal safe set first»), задаёт вопросы вместо работы с тем, что есть, и не даёт воспроизводимых проверок «вход → ожидаемый результат».

Какой информации не хватило: правил репозитория (`SEC-1`…`OBS-1`), порогов (20 000 символов, 10 секунд), формата ответа (`summary`/`risks`/`checks`, ≤ 3 риска), границ роли (только советует) и способа нумерации строк.

## Запуск 2 (P1-02)

Тот же инструмент, `agent: plan`, та же модель и тот же `TRAINING_PR.diff`. В запрос передан [Master Prompt v1](#master-prompt-v1) целиком, других файлов и устных пояснений не было.

**Контроль входа.** Первый прогон из корня репозитория забракован: в нём проверка таймаута повторяла формулировку `504/502 + {"error": {"code", "request_id"}}`, которой нет в master prompt, а есть только в `context.md`. Значит, агент `plan` прочитал файл из репозитория вопреки запрету, и вход перестал быть контролируемым. Прогон повторён из пустой папки, где лежит один `TRAINING_PR.diff`; сохранён второй результат.

```bash
mkdir /tmp/p1-run && cp TRAINING_PR.diff /tmp/p1-run && cd /tmp/p1-run
opencode run "$(cat master_prompt.txt)" --agent plan --model openrouter/openai/gpt-5 --file TRAINING_PR.diff
```

Результат (сжато; поля соответствуют `OUT-1`):

```json
{
  "summary": "PR добавляет ReviewService.review (prompt из diff + синхронный вызов LLM) и POST /api/reviews; косметическое изменение Protocol LLM.",
  "risks": [
    {"file": "app/review_service.py", "line": 14, "evidence": "prompt = f\"Review this pull request and find problems:\\n{diff}\"", "risk": "SEC-1: diff уходит во внешний LLM без удаления секретов."},
    {"file": "app/review_service.py", "line": 15, "evidence": "answer = self.llm.generate(prompt)", "risk": "REL-1: вызов внешнего LLM без timeout и обработки ошибок — возможны зависания и неконтролируемые 500."},
    {"file": "app/api.py", "line": 10, "evidence": "return review_service.review(payload[\"diff\"])", "risk": "API-1: нет проверки длины diff и возврата 413 при >20 000 символов."}
  ],
  "checks": [
    "SEC-1: diff с \"token=TEST_TOKEN_123\" → в prompt [REDACTED]",
    "REL-1: LLM отвечает >10 с → контролируемая ошибка не позже 10 с",
    "API-1: diff длиной 20001 символ → HTTP 413",
    "OUT-1: короткий diff → JSON с summary, risks (≤ 3), checks",
    "SCOPE-1: корректный diff → только советы, без вызовов GitHub API",
    "QA-1: diff без секретов и таймаутов → в risks нет таких пунктов; evidence — дословная строка diff",
    "OBS-1: маркерная строка в diff → в логах только request_id, длительность и статус",
    "Запрос без ключа diff → HTTP 400 (а не 500/KeyError)"
  ]
}
```

### Сравнение запусков

| Признак | P1-01 (только diff) | P1-02 (master prompt) |
|---|---|---|
| Ссылка на файл и строку | Только файл, строк нет | Файл + строка нового файла + дословный `evidence` в каждом риске |
| Вывод подтверждён diff или правилом | Смесь подтверждённого и общих советов (auth, async, Python 3.9); `SEC-1` пропущен | Каждый риск — строка diff + правило (`SEC-1`, `REL-1`, `API-1`); лишних рисков нет |
| Границы помощника | Нарушены: предлагает реализовать изменения (`SCOPE-1`), объём не ограничен | Соблюдены: нет кода и решения о merge, ровно 3 риска, формат `OUT-1` |
| Воспроизводимая проверка | Нет: рекомендации без входа и ожидаемого результата | Есть у каждого пункта: вход → ожидаемый результат |

### Критика P1-02 (моя проверка)

1. **Вытеснение рисков.** Лимит «≤ 3» (`OUT-1`) убрал из `risks` нарушение `OUT-1` (`app/review_service.py:16`, ответ `{"comment": ...}`), причём про отброшенное нигде не сказано: оно видно только в проверках. `KeyError` на `payload["diff"]` тоже попал лишь в checks — и с кодом 400, хотя `problem.md` ожидает 422. В v2: добавить шаг «в `summary` одной фразой назови правила, не вошедшие в топ-3» и зафиксировать 422 как ожидание для невалидного тела.
2. **Якорение на примере.** Формулировка риска `SEC-1` повторяет «хороший пример» из промпта почти дословно, поэтому этот пункт не доказывает самостоятельного поиска; независимо найдены `REL-1` и `API-1`.
3. **Слабые проверки.** Пункты `QA-1` и `SCOPE-1` в checks проверяют свойства самого помощника, а не PR, и малоинформативны для этого diff. Для `API-1` `evidence` (`app/api.py:10`) показывает вызов, а не отсутствие проверки — доказательство держится на правиле, а не на строке.
4. **Нумерация строк — заслуга Context Pack.** Номера 14/15/10 взяты из таблицы фактов в промпте; без неё модель не пересчитывала бы строки сама (заголовок hunk в учебном diff неточен, а пример в `CASE.md` указывает `:16` вместо 14).

Итог: разница между запусками объясняется входом (правила, формат, границы, пример, проверка), а не моделью: инструмент, модель и diff одинаковы.

## RCTF prompt для problem.md (P1-03)

Запуск: `opencode run --agent plan --model openrouter/openai/gpt-5 -f TRAINING_PR.diff -f context.md -f CASE.md -f problem.md "<промпт ниже>"`. Агент `plan` — только анализ, без записи файлов и shell (см. [`OPENCODE_SETUP.md`](../../OPENCODE_SETUP.md)).

```text
ROLE (Роль):
Ты — senior product/requirements-аналитик, который готовит SDLC-артефакты для будущего AI-агента code review (учебный кейс "AI-помощник ревьюера PR"). Твоя задача — не писать код, а улучшить формулировку продуктовой проблемы и метрик.

CONTEXT (Контекст):
- Ниже приложен TRAINING_PR.diff — единственный источник фактов о текущей реализации сервиса.
- Ниже приложен context.md — уже собранный AS IS контекст (продукт, пользователи, процесс, где возникает ошибка/задержка) и Context Pack (факты, правила, форматы, хороший/плохой пример, что пока неизвестно).
- Ниже приложен CASE.md — описание учебного кейса: задача помощника (summary/risks/checks), правила репозитория SEC-1/API-1/REL-1/OUT-1/SCOPE-1/QA-1/OBS-1, хороший/плохой пример комментария.
- Ниже приложен текущий черновик problem.md, который нужно улучшить.
- Работай ТОЛЬКО с приложенными файлами. Не придумывай факты, роли пользователей или метрики, которых нет в diff/context.md/CASE.md. Если данных не хватает — явно пометь как открытый вопрос, а не додумывай.

TASK (Задача):
1. Оцени текущий problem.md по трём критериям и явно перечисли по каждому находки (2-4 пункта):
   a) Пользователь — чётко ли определён (роль, сценарий использования), опирается ли на context.md/CASE.md, а не на общие фразы.
   b) Проблема — подтверждена ли конкретными строками/поведением из diff (KeyError, 500 вместо 422, отсутствие SEC-1/API-1/REL-1), а не общими рассуждениями.
   c) Метрики — измеримы ли (единица измерения, текущее значение или способ замера, целевое значение, когда измеряется, источник данных), относятся ли к правилам репозитория (SEC-1/API-1/REL-1/OUT-1/QA-1/OBS-1), нет ли метрик "для галочки" (VTG/IRR/WACC/CSAT и т.п. без связи с проблемой).
2. Перепиши problem.md целиком в улучшенной версии, сохранив структуру исходного файла (разделы "# Проблема и метрики", "## Проблема", "## Метрики" с той же таблицей-шаблоном, "## Почему изменение метрики подтвердит решение проблемы", "## Как использовали AI").
   - В разделе "Проблема" добавь/уточни: пользователь, ситуация, что происходит сейчас (со ссылкой на конкретное поведение из diff и на нарушаемые правила SEC-1/API-1/REL-1/OUT-1/QA-1/OBS-1 из CASE.md), почему это мешает, что не входит в задачу.
   - В разделе "Метрики" уточни формулировки, чтобы каждая метрика явно была измерима и привязана к конкретному правилу или наблюдаемому поведению из diff.
   - Не трогай раздел "Как использовали AI" — я заполню его сам.

FORMAT (Формат ответа):
1. Блок "Критика черновика" — три подраздела (Пользователь / Проблема / Метрики), пункты списком, без воды.
2. Блок "Открытые вопросы" — то, что нельзя подтвердить приложенными файлами (если такое есть).
3. Блок "Улучшенный problem.md" — готовый markdown-текст, который можно целиком вставить в файл (без раздела "Как использовали AI").
Никаких общих советов вида "добавьте тесты" без привязки к конкретному правилу или строке diff.
```

Три слабых места ответа:

1. Модель не заметила, что `CASE.md` явно запрещает переносить в Context Pack правила `snake_case` для БД и i18n — этого и не требовалось, но подтверждает, что модель ограничилась только релевантными пунктами, а не скопировала всё правило целиком (это скорее сильная сторона, отмечаю как проверку границ).
2. В блоке «Открытые вопросы» модель не предложила, где физически искать ответы (например, у кого уточнить формат ошибки REL-1) — просто перечислила пробелы, без next steps.
3. Раздел «Метрики» для `OBS-1` и `SEC-1` сформулирован как бинарный (100%/0%), без промежуточных порогов — на реальном проекте это может быть слишком жёстким KPI для первого релиза.

Какой информации не хватило: точных требований к формату ошибки при таймауте LLM и точного набора паттернов для маскирования секретов — оба пункта отсутствуют в приложенных файлах, модель корректно вынесла их в «Открытые вопросы», а не придумала.

## Критика RCTF-промпта P1-03 и исправленный контракт

Разбор промпта из раздела «RCTF prompt для problem.md (P1-03)». Исправленная версия пока **не запускалась**, поэтому строки в журнале выше для неё нет.

### Поиск двусмысленности

- В TASK сказано «без воды» и «не опирается ли на общие фразы». LLM не умеет измерять консистенцию «воды» и субъективность «общих фраз», поэтому стиль текста будет непредсказуемым.
- В FORMAT запрашивается «готовый markdown-текст, который можно целиком вставить в файл (без раздела "Как использовали AI")». Это ломает сценарий полного копирования текста в буфер обмена.

### Поиск непроверяемых слов

- «Перепиши problem.md целиком в улучшенной версии». Критерий «улучшенности» не формализован.
- «Чётко ли определён». У модели нет встроенного тумблера чёткости.

### Пропущенные edge cases

- Промпт жёстко требует «2-4 пункта» находок по каждому критерию. Если черновик уже идеален, модель начнёт высасывать проблемы из пальца, чтобы выполнить квоту.
- Не задан приоритет источников. Если `context.md` и `CASE.md` противоречат друг другу в бизнес-логике, агент может выбрать неверный источник.

### Опасные действия

- Инструкция «Не трогай раздел "Как использовали AI" — я заполню его сам» и запрет выводить его в финальном блоке приведут к тому, что при копировании всего ответа в `problem.md` (Ctrl+A → Ctrl+V) пользователь затрёт этот раздел и забудет его заполнить. Модель должна возвращать структуру целиком с плейсхолдером для ручного заполнения.

### Исправленный контракт (Master Prompt)

```text
ROLE (Роль):
Ты — senior product/requirements-аналитик, готовящий SDLC-артефакты для AI-агента code review.

CONTEXT (Контекст):
- Источники фактов (приоритет от высшего к низшему при конфликтах): 1. TRAINING_PR.diff, 2. CASE.md, 3. context.md.
- Текущий черновик: problem.md.
- Работай ТОЛЬКО с приложенными файлами. Запрещено использовать метрики, правила или факты, отсутствующие в этих текстах.

TASK (Задача):
1. Проверь текущий problem.md на соответствие трём бинарным правилам:
   a) Пользователь: указаны ли конкретная роль и сценарий из context.md/CASE.md.
   b) Проблема: есть ли прямая ссылка на строку или поведение из TRAINING_PR.diff.
   c) Метрики: содержит ли каждая метрика в таблице столбцы с единицей измерения, способом замера и явной привязкой к правилам (SEC-1, API-1, REL-1, OUT-1, QA-1, OBS-1).
2. Если правило нарушено, укажи, чего именно не хватает. Если нарушений нет — напиши "Соответствует". Не придумывай ошибки, если текст удовлетворяет правилу.
3. Сгенерируй финальный текст problem.md:
   - В разделе "Проблема" укажи пользователя, ситуацию, текущее поведение (с указанием нарушенного правила) и ограничения задачи.
   - В разделе "Метрики" заполни таблицу только проверяемыми показателями, связанными с diff.
   - Обязательно выведи заголовок "## Как использовали AI" и под ним напиши ровно одну строку: `<!-- Заполняется человеком -->`.

FORMAT (Формат ответа):
1. Блок "Аудит черновика": список найденных нарушений по 3 правилам с указанием конкретных строк.
2. Блок "Открытые вопросы": список недостающих фактов (только если их невозможно найти в приложенных файлах).
3. Блок "Финальный problem.md": полный итоговый код файла в блоке ```markdown.
```

### Что ещё стоит поправить в v2 (мои замечания)

1. **Плейсхолдер затрёт заполненный раздел.** В текущем `problem.md` раздел «Как использовали AI» уже заполнен. По контракту модель выведет вместо него `<!-- Заполняется человеком -->`, и вставка ответа целиком его уничтожит. Надёжнее: «раздел "Как использовали AI" перенеси из входного файла дословно; если он пуст — оставь плейсхолдер».
2. **Контракт покрывает не все восемь вопросов master prompt.** Нет рабочих шагов, условия остановки, проверки утверждений (как в CoVe/ReAct из P1-04) и Definition of Done. Достаточно добавить раздел «Проверка: у каждой ссылки на строку процитируй строку из diff» и DoD.
3. **Приоритет источников нужно объяснить.** У `TRAINING_PR.diff` высший приоритет для фактов о коде, но правила (`SEC-1`…`OBS-1`) есть только в `CASE.md`. Стоит разделить: «факты о коде — из diff, правила — из CASE.md, допущения — из context.md; при конфликте фактов о коде побеждает diff».
4. **Метрики «только связанные с diff» слишком узки.** `OBS-1` в diff не проявляется (логирования там нет), но правило из `CASE.md` метрику допускает. Лучше сформулировать «связанные с diff или правилом из CASE.md».

## ReAct prompt для problem.md (P1-04)

Запуск: `opencode run --agent plan --model openrouter/openai/gpt-5 -f TRAINING_PR.diff -f context.md -f CASE.md -f problem.md "<промпт ниже>"`.

```text
Ты — верификатор SDLC-артефакта. Работай в цикле ReAct: Thought → Action → Observation → Conclusion. Не пропускай шаги и не давай финальный вывод без явного Observation.

ИСТОЧНИКИ ИСТИНЫ (только они, ничего не придумывай):
- TRAINING_PR.diff — код PR.
- CASE.md — правила репозитория (SEC-1, API-1, REL-1, OUT-1, SCOPE-1, QA-1, OBS-1) и задача помощника.
- context.md — ранее собранный AS IS контекст.
Проверяемый документ: problem.md (приложен).

ЗАДАЧА:
Пройди по problem.md пункт за пунктом (каждый буллет раздела "Проблема" и каждую строку таблицы "Метрики") и для КАЖДОГО выполни ReAct-цикл:

Thought: что именно нужно проверить в этом утверждении (факт, ссылка на строку, номер правила).
Action: какой источник и что именно в нём ты проверяешь (например: "читаю TRAINING_PR.diff, ищу app/api.py вокруг строки 37" или "читаю CASE.md, ищу определение REL-1").
Observation: что реально написано в источнике (процитируй релевантный фрагмент).
Conclusion: один из вариантов —
  - CONFIRMED — утверждение точно подтверждено источником, ничего менять не нужно;
  - CONFIRMED WITH FIX — по сути верно, но формулировку/номер строки/номер правила нужно поправить (укажи точную правку);
  - NOT CONFIRMED — источник не подтверждает утверждение; предложи либо убрать пункт, либо перенести в "Открытые вопросы".

После прохода по всем пунктам:
1. Дай таблицу "Итог верификации": Пункт | Conclusion | Правка (если есть).
2. Дай финальный блок "Исправленный problem.md" — но включи в него ТОЛЬКО те правки, которые следуют из Conclusion выше (не переписывай то, что уже CONFIRMED). Сохрани структуру файла как есть.

ОГРАНИЧЕНИЯ:
- Не придумывай новых фактов и не добавляй новые правила, которых нет в CASE.md.
- Если для проверки не хватает данных — это NOT CONFIRMED с пометкой "недостаточно данных", а не CONFIRMED по умолчанию.
- Не сокращай ReAct-цикл до одной строки — каждый шаг (Thought/Action/Observation/Conclusion) отдельно.
```

Три слабых места ответа:

1. **Ложноположительная «правка».** Модель сама заявила Thought/Action/Observation и пришла к выводу CONFIRMED WITH FIX для ссылки `app/api.py:36–37` → `35–37`, но при этом её же Observation процитировала правильные строки (`def create_review` / `return review_service.review(payload["diff"])`), а строку 35 (декоратор `@app.post`) в Observation вообще не упомянула — то есть Conclusion не следовал из собственного Observation модели. Перепроверка `nl -ba TRAINING_PR.diff` подтвердила: `36–37` были верны изначально. Это ключевой урок ReAct-проверки: даже при видимом step-by-step рассуждении Conclusion нужно сверять с Observation, а не доверять на слово.
2. **Излишне строгая (но обоснованная) буквальность.** Пункт «Почему это мешает пользователю или бизнесу» помечен NOT CONFIRMED, потому что источники не содержат этой формулировки дословно — формально верно, но это аналитический вывод автора, а не факт для цитирования; модель не предложила альтернативу («пометить как вывод»), а сразу — «убрать».
3. **Не предложила done-условие для «Открытых вопросов».** Как и в RCTF-запуске, ReAct-проход перечисляет пробелы (например, по `OBS-1`), но не говорит, у кого/как их закрыть.

Какой информации не хватило: модели неоткуда было узнать, есть ли логирование вне двух показанных файлов diff — это корректно и осталось в «Открытых вопросах», а не было додумано.
